services:
  - type: web
    name: dialaw-tv-backend
    env: node
    buildCommand: npm install && npm rebuild better-sqlite3
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
